{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:bphr:schema:relation-packet",
  "title": "Relation Packet",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id",
    "status",
    "theme_id",
    "relation_type",
    "directionality",
    "from_anchor",
    "to_anchor",
    "source_refs",
    "created_by"
  ],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^rel_[a-z0-9][a-z0-9_-]*$"
    },
    "status": {
      "type": "string",
      "enum": ["draft", "needs_revision", "approved", "rejected"]
    },
    "theme_id": {
      "type": "string",
      "pattern": "^theme_[a-z0-9][a-z0-9_-]*$"
    },
    "relation_type": {
      "type": "string",
      "enum": ["thematic_link", "pattern", "contrast", "development", "echo", "other"]
    },
    "directionality": {
      "type": "string",
      "enum": ["directional", "bidirectional"]
    },
    "from_anchor": {
      "$ref": "#/$defs/passage_anchor"
    },
    "to_anchor": {
      "$ref": "#/$defs/passage_anchor"
    },
    "rationale": {
      "type": "string"
    },
    "source_refs": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/source_ref"
      }
    },
    "insight_packet_ids": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^insight_[a-z0-9][a-z0-9_-]*$"
      },
      "uniqueItems": true
    },
    "created_by": {
      "type": "string",
      "minLength": 1
    },
    "reviewed_by": {
      "type": "string"
    },
    "review_notes": {
      "type": "string"
    }
  },
  "$defs": {
    "passage_anchor": {
      "type": "object",
      "additionalProperties": false,
      "required": ["translation", "book", "start_chapter", "start_verse", "end_chapter", "end_verse"],
      "properties": {
        "translation": {
          "type": "string",
          "const": "BSB"
        },
        "book": {
          "type": "string",
          "minLength": 1
        },
        "start_chapter": {
          "type": "integer",
          "minimum": 1
        },
        "start_verse": {
          "type": "integer",
          "minimum": 1
        },
        "end_chapter": {
          "type": "integer",
          "minimum": 1
        },
        "end_verse": {
          "type": "integer",
          "minimum": 1
        }
      }
    },
    "source_ref": {
      "type": "object",
      "additionalProperties": false,
      "required": ["source_id", "span"],
      "properties": {
        "source_id": {
          "type": "string",
          "pattern": "^src_[a-z0-9][a-z0-9_-]*$"
        },
        "span": {
          "$ref": "#/$defs/source_span"
        },
        "notes": {
          "type": "string"
        }
      }
    },
    "source_span": {
      "type": "object",
      "additionalProperties": false,
      "required": ["start_seconds", "end_seconds"],
      "properties": {
        "start_seconds": {
          "type": "number",
          "minimum": 0
        },
        "end_seconds": {
          "type": "number",
          "exclusiveMinimum": 0
        }
      }
    }
  }
}
